---
import { getToken } from "@/lib/auth";
import { getUserDataByToken } from "@/services/auth";
import { OsucPermissions } from "@/types/permissions";
import EditorMDX from "@/components/EditorMDX.tsx";
import Layout from "@/layouts/Layout.astro";

// Authentication check
const token = getToken(Astro.cookies);
if (!token) {
  return Astro.redirect("/");
}

const user = await getUserDataByToken(token);
console.log(user?.permissions);
if (!user || !user.permissions.includes(OsucPermissions.userCanCreateBlogs)) {
  return Astro.redirect("/");
}

let message = "";
let messageType = "";

if (Astro.request.method === "POST") {
  try {
    const data = await Astro.request.formData();

    // Enviar los datos a la API
    const response = await fetch(new URL("/api/savemdx", Astro.url.origin), {
      method: "POST",
      body: data,
    });

    if (response.ok) {
      message = "Recomendación creada exitosamente";
      messageType = "success";
    } else {
      message =
        "Error al crear la recomendación. Por favor, intenta nuevamente.";
      messageType = "error";
    }
  } catch (error) {
    console.error("Error al procesar el formulario:", error);
    message = "Error al procesar el formulario. Por favor, intenta nuevamente.";
    messageType = "error";
  }
}

const blogsResponse = await fetch(new URL("/api/blogs", Astro.url.origin));
const blogsData = (await blogsResponse.json()) as { schools?: string[] };
const schools = blogsData.schools || [];
---

<Layout>
  <main
    class="w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 sm:py-6 lg:py-8"
  >
    <!-- Header Section -->
    <header class="mb-8 sm:mb-12">
      <section
        class="border border-border rounded-md p-4 sm:p-6 lg:p-8 min-w-0"
        aria-labelledby="page-title"
      >
        <div class="text-center">
          <h1
            id="page-title"
            class="text-2xl sm:text-3xl lg:text-4xl xl:text-5xl font-bold text-foreground mb-2 break-words"
          >
            Crear una nueva recomendación
          </h1>
          <p class="text-sm sm:text-base lg:text-lg text-muted-foreground">
            Comparte tus experiencias y conocimientos con la comunidad
            estudiantil
          </p>
        </div>
      </section>
    </header>

    <!-- Consejos y Componentes en fila -->
    <div
      class="grid grid-cols-1 lg:grid-cols-2 gap-4 sm:gap-6 lg:gap-8 mb-8 sm:mb-12 min-w-0"
    >
      <!-- Consejos para escribir -->
      <section
        class="border border-border rounded-md p-4 sm:p-6 min-w-0"
        aria-labelledby="tips-title"
      >
        <h3
          id="tips-title"
          class="text-base sm:text-lg font-semibold text-foreground mb-3 sm:mb-4"
        >
          💡 Consejos para escribir
        </h3>
        <ul
          class="text-xs sm:text-sm text-muted-foreground space-y-2"
          role="list"
        >
          <li class="flex items-start gap-2">
            <span class="text-green-500 mt-1 flex-shrink-0">•</span>
            <span class="min-w-0">Usa un título descriptivo y atractivo</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-green-500 mt-1 flex-shrink-0">•</span>
            <span class="min-w-0"
              >Estructura tu contenido con encabezados y párrafos cortos</span
            >
          </li>
          <li class="flex items-start gap-2">
            <span class="text-green-500 mt-1 flex-shrink-0">•</span>
            <span class="min-w-0"
              >Incluye ejemplos y experiencias personales</span
            >
          </li>
          <li class="flex items-start gap-2">
            <span class="text-green-500 mt-1 flex-shrink-0">•</span>
            <span class="min-w-0"
              >Revisa la ortografía y gramática antes de publicar</span
            >
          </li>
        </ul>
      </section>

      <!-- Información sobre componentes -->
      <section
        class="border border-border rounded-md p-4 sm:p-6 min-w-0"
        aria-labelledby="components-title"
      >
        <h3
          id="components-title"
          class="text-base sm:text-lg font-semibold text-foreground mb-3 sm:mb-4"
        >
          🧩 Componentes disponibles
        </h3>
        <div class="space-y-2 sm:space-y-3">
          <div class="p-2 sm:p-3 bg-muted/50 rounded-lg min-w-0">
            <h4 class="font-medium text-xs sm:text-sm text-foreground mb-1">
              Pills
            </h4>
            <p class="text-xs text-muted-foreground">
              Etiquetas coloridas para destacar información importante
            </p>
          </div>
          <div class="p-2 sm:p-3 bg-muted/50 rounded-lg min-w-0">
            <h4 class="font-medium text-xs sm:text-sm text-foreground mb-1">
              Tablas
            </h4>
            <p class="text-xs text-muted-foreground">
              Organiza datos de manera clara y estructurada
            </p>
          </div>
          <div class="p-2 sm:p-3 bg-muted/50 rounded-lg min-w-0">
            <h4 class="font-medium text-xs sm:text-sm text-foreground mb-1">
              Imágenes
            </h4>
            <p class="text-xs text-muted-foreground">
              Añade imágenes para complementar tu contenido
            </p>
          </div>
        </div>
      </section>
    </div>

    <!-- Formulario Principal -->
    <section aria-labelledby="form-title">
      <article class="border border-border rounded-md min-w-0">
        <div class="p-4 sm:p-6 lg:p-8">
          <h2
            id="form-title"
            class="text-lg sm:text-xl font-semibold mb-4 sm:mb-6"
          >
            Información de la recomendación
          </h2>

          <form class="space-y-4 sm:space-y-6 min-w-0" method="POST">
            <!-- Información básica de la recomendación -->
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
              <div class="space-y-2 min-w-0">
                <label class="text-sm font-medium text-foreground block">
                  Título de la recomendación <span class="text-red-500">*</span>
                </label>
                <input
                  name="title"
                  placeholder="Ingrese el título de la recomendación"
                  class="w-full min-w-0 px-3 sm:px-4 py-2 sm:py-3 border border-border rounded-lg bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-all duration-200"
                  required
                />
              </div>
              <div class="space-y-2 min-w-0">
                <label
                  >Nombre de la iniciativa: <span class="text-red-500">*</span
                  ></label
                >
                <input
                  name="name"
                  placeholder="Ingrese el nombre de la iniciativa"
                  class="w-full min-w-0 px-3 sm:px-4 py-2 sm:py-3 border border-border rounded-lg bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-all duration-200"
                  required
                />
              </div>
              <div class="space-y-2 min-w-0">
                <label class="text-sm font-medium text-foreground block">
                  Tiempo de lectura <span class="text-red-500">*</span>
                </label>
                <div class="relative min-w-0">
                  <input
                    name="readtime"
                    type="number"
                    min="1"
                    max="60"
                    placeholder="5"
                    class="w-full min-w-0 px-3 sm:px-4 py-2 sm:py-3 pr-12 border border-border rounded-lg bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-all duration-200 [appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none"
                    required
                    oninput="this.value = this.value.replace(/[^0-9]/g, '').replace(/^0+/, '') || ''"
                  />
                  <span
                    class="absolute right-3 top-1/2 transform -translate-y-1/2 text-muted-foreground text-sm"
                  >
                    min
                  </span>
                </div>
              </div>
              <div class="space-y-2 min-w-0">
                <label class="text-sm font-medium text-foreground block">
                  Facultad <span class="text-red-500">*</span>
                </label>
                <select
                  name="faculty"
                  class="w-full min-w-0 px-3 sm:px-4 py-2 sm:py-3 border border-border rounded-lg bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-all duration-200"
                  required
                >
                  <option value="" disabled selected
                    >Seleccione una facultad</option
                  >
                  {
                    schools.map((school) => (
                      <option value={school}>{school}</option>
                    ))
                  }
                </select>
              </div>
              <div class="space-y-2 min-w-0">
                <label class="text-sm font-medium text-foreground block">
                  Código del curso <span class="text-red-500">*</span>
                </label>
                <input
                  name="code"
                  placeholder="Ingrese el código del curso"
                  class="w-full min-w-0 px-3 sm:px-4 py-2 sm:py-3 border border-border rounded-lg bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-all duration-200"
                  required
                />
              </div>
              <div class="space-y-2 min-w-0">
                <label class="text-sm font-medium text-foreground block"
                  >Clasificación <span class="text-red-500">*</span></label
                >
                <input
                  type="number"
                  min="1"
                  max="5"
                  name="qualification"
                  placeholder="Ingrese la clasificación del curso (1-5)"
                  class="w-full min-w-0 px-3 sm:px-4 py-2 sm:py-3 border border-border rounded-lg bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-all duration-200"
                  required
                />
              </div>
            </div>

            <!-- Resumen de la recomendación - Ocupa toda la fila -->
            <div class="space-y-2 min-w-0">
              <label class="text-sm font-medium text-foreground block">
                Resumen de la recomendación <span class="text-red-500">*</span>
              </label>
              <textarea
                name="resume"
                placeholder="Breve descripción del contenido de la recomendación. Explica de qué trata tu artículo y qué pueden esperar los lectores."
                rows="4"
                class="w-full min-w-0 px-3 sm:px-4 py-2 sm:py-3 border border-border rounded-lg bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:border-transparent transition-all duration-200 resize-none"
                required></textarea>
            </div>

            <!-- Separador -->
            <div class="border-t border-border pt-4 sm:pt-6">
              <div class="space-y-2 mb-4">
                <label class="text-sm font-medium text-foreground block">
                  Contenido de la recomendación <span class="text-red-500"
                    >*</span
                  >
                </label>
                <p class="text-xs sm:text-sm text-muted-foreground">
                  Utiliza el editor para crear contenido enriquecido con
                  formato, imágenes y componentes especiales.
                </p>
              </div>
            </div>

            <!-- Editor MDX -->
            <div class="min-w-0">
              <EditorMDX
                submitText="Publicar Recomendación"
                client:only="react"
              />
            </div>
            <input
              type="text"
              value="recommendations"
              name="contentType"
              hidden
            />
          </form>
        </div>
      </article>
    </section>
  </main>
</Layout>
